services:
  # Go WebSocket Server
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: lgtm-server
    ports:
      - "8081:8081"
    restart: unless-stopped
    networks:
      - lgtm-network
    volumes:
      # Mount tasks.json for easy editing without rebuild
      - ./server/tasks.json:/root/tasks.json:ro
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8081"]
      interval: 30s
      timeout: 10s
      retries: 3

  # React Frontend (Bun + Nginx)
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: lgtm-client
    ports:
      - "3001:80"
    depends_on:
      - server
    restart: unless-stopped
    networks:
      - lgtm-network

networks:
  lgtm-network:
    driver: bridge
